<UserControl x:Class="TaskBoard.Client.UI.Controls.ColumnControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 BorderThickness="3,2,0,0" BorderBrush="Silver"
			 Width="240">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<Label Grid.Row="0" Name="LabelColumnHeader" HorizontalContentAlignment="Center">
			<Label.ContextMenu>
				<ContextMenu>
					<MenuItem Header="Добавить задачу" />
					<MenuItem Header="Добавить столбец" />
					<MenuItem Header="Редактировать столбец" />
					<MenuItem Header="Удалить столбец" />
				</ContextMenu>
			</Label.ContextMenu>
		</Label>

		<Rectangle Grid.Row="1" Height="1" Fill="Silver" />

		<StackPanel Grid.Row="2" Name="StackPanelTaskControls" />
	</Grid>
</UserControl>