<UserControl x:Class="TaskBoard.Client.UI.Views.Controls.BoardControlView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:viewControls="clr-namespace:TaskBoard.Client.UI.Views.Controls"
			 mc:Ignorable="d"
			 d:DesignHeight="300" d:DesignWidth="800"
			 d:DataContext="{Binding Source={StaticResource BoardViewModel}}">
	<ItemsControl ItemsSource="{Binding ColumnControlViewModels, UpdateSourceTrigger=PropertyChanged}">
		<ItemsControl.ItemsPanel>
			<ItemsPanelTemplate>
				<StackPanel Orientation="Horizontal" />
			</ItemsPanelTemplate>
		</ItemsControl.ItemsPanel>
		<ItemsControl.ItemTemplate>
			<DataTemplate>
				<viewControls:ColumnControlView Content="{Binding}" Width="250" />
			</DataTemplate>
		</ItemsControl.ItemTemplate>
	</ItemsControl>
</UserControl>