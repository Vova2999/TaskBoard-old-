<UserControl x:Class="TaskBoard.Client.UI.Views.Controls.TaskControlView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:viewModelControls="clr-namespace:TaskBoard.Client.UI.ViewModels.Controls"
			 mc:Ignorable="d"
			 d:DesignHeight="300" d:DesignWidth="300"
			 d:DataContext="{d:DesignInstance Type=viewModelControls:TaskControlViewModel}">
	<StackPanel>
		<Label Content="{Binding Path=TaskModel.Header, UpdateSourceTrigger=PropertyChanged}" />
		<Label Content="{Binding Path=TaskModel.Description, UpdateSourceTrigger=PropertyChanged}" />
		<Label Content="{Binding Path=TaskModel.ColumnHeader, UpdateSourceTrigger=PropertyChanged}" />
	</StackPanel>
</UserControl>