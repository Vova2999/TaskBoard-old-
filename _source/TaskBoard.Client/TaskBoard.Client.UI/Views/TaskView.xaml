<UserControl x:Class="TaskBoard.Client.UI.Views.TaskView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d"
			 d:DesignHeight="300" d:DesignWidth="300"
			 d:DataContext="{task:TaskViewModel}">
	<StackPanel>
		<Label Content="{Binding Path=TaskModel.Header}" />
		<Label Content="{Binding Path=TaskModel.Description}" />
		<Label Content="{Binding Path=TaskModel.Column.Header}" />
	</StackPanel>
</UserControl>