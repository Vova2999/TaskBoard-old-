<Window x:Class="TaskBoard.Client.UI.Views.MainView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
		xmlns:views="clr-namespace:TaskBoard.Client.UI.Views"
		Title="MainView" Height="600" Width="1200"
		DataContext="{Binding MainViewModel, Source={StaticResource Locator}}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<ComboBox Grid.Row="0" ItemsSource="{Binding BoardModels, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding BoardViewModel.CurrentBoardModel, Mode=TwoWay}">
			<i:Interaction.Triggers>
				<i:EventTrigger EventName="DropDownOpened">
					<i:InvokeCommandAction Command="{Binding LoadBoardsCommand}" />
				</i:EventTrigger>
			</i:Interaction.Triggers>
			<ComboBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding Name}" />
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>

		<views:BoardView Grid.Row="1" DataContext="{Binding BoardViewModel}" />
	</Grid>
</Window>