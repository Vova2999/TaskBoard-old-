<UserControl x:Class="TaskBoard.Client.UI.Views.ColumnView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:viewModels="clr-namespace:TaskBoard.Client.UI.ViewModels"
			 xmlns:views="clr-namespace:TaskBoard.Client.UI.Views"
			 mc:Ignorable="d"
			 d:DesignHeight="300" d:DesignWidth="300">
	<d:UserControl.DataContext>
		<viewModels:ColumnViewModel />
	</d:UserControl.DataContext>
	<UserControl.Resources>
		<DataTemplate x:Key="StackPanelDataTemplate">
			<StackPanel Orientation="Horizontal">
				<views:ColumnView DataContext="{Binding}" />
			</StackPanel>
		</DataTemplate>
	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<Label Grid.Row="0" Content="{Binding ColumnModel.Header}" HorizontalContentAlignment="Center" />
		<Rectangle Grid.Row="1" Height="1" Fill="{Binding ColumnModel.Brush}" />
		<ItemsControl Grid.Row="2" ItemTemplate="{StaticResource StackPanelDataTemplate}" ItemsSource="{Binding Tasks, UpdateSourceTrigger=PropertyChanged}" />
	</Grid>
</UserControl>