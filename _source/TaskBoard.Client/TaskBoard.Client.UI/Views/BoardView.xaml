<UserControl x:Class="TaskBoard.Client.UI.Views.BoardView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:views="clr-namespace:TaskBoard.Client.UI.Views"
			 mc:Ignorable="d"
			 d:DesignHeight="300" d:DesignWidth="800"
			 d:DataContext="{Binding Source={StaticResource BoardViewModel}}">
	<ItemsControl ItemsSource="{Binding ColumnViewModels, UpdateSourceTrigger=PropertyChanged}">
		<ItemsControl.ItemsPanel>
			<ItemsPanelTemplate>
				<StackPanel Orientation="Horizontal" />
			</ItemsPanelTemplate>
		</ItemsControl.ItemsPanel>
		<ItemsControl.ItemTemplate>
			<DataTemplate>
				<views:ColumnView Content="{Binding}" Width="250" />
			</DataTemplate>
		</ItemsControl.ItemTemplate>
	</ItemsControl>
</UserControl>